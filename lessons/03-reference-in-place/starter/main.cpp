#include <iostream>

// TODO: ApplyGainInPlace(float& sample, float gain)

int main() {
    float sample = 0.25f;
    std::cout << "before: " << sample << '\n';

    // TODO: gain=2.0fで呼ぶ

    std::cout << "after : " << sample << '\n';
}
