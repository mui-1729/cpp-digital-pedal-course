#include "daisy_seed.h"

using namespace daisy;
using namespace daisy::seed;

DaisySeed hw;

int main() {
    hw.Init();
    // TODO: button D0, LED D1 を初期化
    while (true) {
        // TODO: buttonを読んでLEDへ反映
    }
}
