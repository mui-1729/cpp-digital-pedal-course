# Solution note

`main.cpp`は2026-09-15時点のlibDaisy公開APIに合わせたreference implementationです。

実際のLesson 19では、Lesson 15で記録したSeed3/libDaisy versionでcompileし、API差分があれば**その時点の公式docsを優先**して更新します。

`PedalEngine.h`はLesson 14のPC版と同じです。ここがこの教材の重要ポイントです。
